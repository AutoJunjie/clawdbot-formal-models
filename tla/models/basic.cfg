SPECIFICATION Spec

CONSTANTS
  Users = {u1}
  Sessions = {sMain, sShared}
  Providers = {discord, signal}
  Tools = {"memory_get", "memory_search", "message.send", "gateway.config.apply", "gateway.update.run", "browser.act", "exec"}

  MainSession = sMain
  SharedSession = sShared

  \* Categorize tools by risk / information flow
  MemTools = {"memory_get", "memory_search"}
  ExternalTools = {"message.send", "gateway.config.apply", "gateway.update.run", "browser.act"}
  ElevatedTools = {"exec"}
